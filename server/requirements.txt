# Consolidated requirements entrypoint.
#
# Usage examples:
#   pip install -r requirements/base.txt        # runtime only
#   pip install -r requirements/dev.txt         # local development
#   pip install -r requirements/prod.txt        # production images
#   pip install -r requirements.txt             # default (runtime)

-r requirements/base.txt
